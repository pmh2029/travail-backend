# API Service
API_PORT=

# Database Image
# PostgreSQL: DB_IMAGE=postgres:15
# MySQL     : DB_IMAGE=mysql:8.0.26
DB_IMAGE=postgres:15

# DB Info
# PostgreSQL: DB_PORT=5432
# MySQL:      DB_PORT=3306
DB_PORT=
DB_USER=
DB_NAME=
DB_PASS=
DB_HOST=<if you run in localhost DB_HOST=localhost>

# Network 
HOST=<if you run in localhost HOST=host>

JWT_SECRET_KEY=

# OAuth2 service
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

tmp/
.env
